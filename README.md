# eslint config

## Usage

In the `.eslintrc.js` file:

```
module.exports = {
  'extends': 'kswedberg'
};
```

If you don't want es6 or react features:

```
module.exports = {
  'extends': 'kswedberg/es5'
};
```
